/**
 * Total user-visible numbered steps (mirrored from VISIBLE_STEP_COUNT).
 */
export const TOTAL_WIZARD_STEPS = 5;

/** Delay after BLE disconnect before reconnecting (ms). */
export const DISCONNECT_SETTLE_MS = 500;
