/** Default polling interval while waiting for WiFi connection (ms) */
export const DEFAULT_POLL_INTERVAL_MS = 2000;

/** Default polling timeout (ms) */
export const DEFAULT_POLL_TIMEOUT_MS = 30000;

/** Default network priority when adding via provisioning flow */
export const DEFAULT_NETWORK_PRIORITY = 10;

/** Total number of wizard steps (excluding 'manage') */
export const TOTAL_WIZARD_STEPS = 6;

/** Delay after BLE disconnect before reconnecting (ms) */
export const DISCONNECT_SETTLE_MS = 500;
